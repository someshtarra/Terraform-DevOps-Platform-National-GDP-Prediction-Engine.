apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gdp-prediction-production
  namespace: argocd
  labels:
    environment: production
spec:
  project: default
  source:
    repoURL: 'https://github.com/someshtarra/TERRAFORM.git'
    targetRevision: HEAD
    path: helm/gdp-prediction-app
    helm:
      valueFiles:
        - values-production.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: gdp-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
